version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - pip install -r requirements.txt
run:
  command: streamlit run Pega_Log_Troubleshooter.py --server.port 8080 --server.headless true